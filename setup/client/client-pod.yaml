apiVersion: v1
kind: Pod
metadata:
  name: mlperf-client
spec:
  containers:
  - name: client
    image: python:3.12-slim
    command: ["sleep", "infinity"]
    securityContext:
      privileged: true
  restartPolicy: Never

